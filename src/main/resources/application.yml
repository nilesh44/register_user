server:
  port: 8080

    
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/login_mgt_2
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

applicationConstant:
  sessionTimeout: 120000
